name: One-shot APP runtime patch

on:
  push:
    branches:
      - feature/hermes-multi-agent
    paths:
      - scripts/patch_hub_runtime_cleanup.py
      - .github/workflows/patch-app-runtime.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/hermes-multi-agent
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Apply product patch
        run: python scripts/patch_hub_runtime_cleanup.py
      - name: Verify syntax and diff
        run: |
          python -m compileall -q src/openakita/api/routes/hub.py
          git diff --check
      - name: Commit product patch
        run: |
          git config user.name "openakita-maintenance-bot"
          git config user.email "actions@users.noreply.github.com"
          git add src/openakita/api/routes/hub.py
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "fix: clean temporary Agent batch exports"
          git pull --rebase origin feature/hermes-multi-agent
          git push origin HEAD:feature/hermes-multi-agent

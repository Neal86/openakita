name: One-shot connector generation hardening

on:
  push:
    branches:
      - feature/hermes-multi-agent
    paths:
      - scripts/patch_connector_generation.py
      - .github/workflows/patch-connector-generation.yml
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/hermes-multi-agent
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Apply generation patch
        run: python scripts/patch_connector_generation.py
      - name: Verify generation isolation
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install -e ./openakita-plugin-sdk
          ruff check src/openakita/wechat_desktop/manager.py src/openakita/windows_connector/manager.py src/openakita/api/routes/wechat_desktop.py tests/wechat_desktop/test_connection_generation.py
          pytest -q tests/wechat_desktop/test_connection_generation.py tests/windows_connector tests/wechat_desktop tests/hermes
          git diff --check
      - name: Commit generation hardening
        run: |
          git config user.name "openakita-maintenance-bot"
          git config user.email "actions@users.noreply.github.com"
          git add src/openakita/wechat_desktop/manager.py src/openakita/windows_connector/manager.py src/openakita/api/routes/wechat_desktop.py tests/wechat_desktop/test_connection_generation.py
          if git diff --cached --quiet; then
            echo "Generation hardening already applied."
            exit 0
          fi
          git commit -m "fix(connector): reject stale websocket generations"
          git pull --rebase origin feature/hermes-multi-agent
          git push origin HEAD:feature/hermes-multi-agent

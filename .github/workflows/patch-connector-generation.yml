name: One-shot safety hardening

on:
  push:
    branches:
      - feature/hermes-multi-agent
    paths:
      - scripts/patch_agent_package_runtime_safety.py
      - scripts/patch_profile_categories_safety.py
      - scripts/patch_hub_io_safety.py
      - .github/workflows/patch-connector-generation.yml
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/hermes-multi-agent
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Apply static-review safety patches
        run: |
          python scripts/patch_agent_package_runtime_safety.py
          python scripts/patch_profile_categories_safety.py
          python scripts/patch_hub_io_safety.py
      - name: Commit static-review safety hardening
        run: |
          git config user.name "openakita-maintenance-bot"
          git config user.email "actions@users.noreply.github.com"
          git add src/openakita/agents/packager.py src/openakita/agents/profile.py src/openakita/api/routes/hub.py
          if git diff --cached --quiet; then
            echo "Static-review safety hardening already applied."
            exit 0
          fi
          git commit -m "fix: harden Agent package and Hub persistence"
          git pull --rebase origin feature/hermes-multi-agent
          git push origin HEAD:feature/hermes-multi-agent

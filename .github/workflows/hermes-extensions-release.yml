name: Hermes Extensions release

on:
  workflow_dispatch:
  push:
    branches:
      - feature/hermes-extensions
    tags:
      - "hermes-extensions-v*"

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Read version
        id: version
        run: |
          VERSION=$(python - <<'PY'
          from pathlib import Path
          for line in Path('hermes-extensions/plugin.yaml').read_text('utf-8').splitlines():
              if line.startswith('version:'):
                  print(line.split(':',1)[1].strip().strip('"').strip("'"))
                  break
          PY
          )
          test -n "$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install and validate source
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r hermes-extensions/requirements.txt pytest ruff fastapi pydantic
          python -m compileall -q hermes-extensions
          ruff check hermes-extensions
          pytest -q hermes-extensions/tests
          node --check hermes-extensions/dashboard/src/index.js

      - name: Build isolated release tree
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          OUT="release/hermes-extensions-v${VERSION}"
          mkdir -p "$OUT"
          rsync -a hermes-extensions/ "$OUT/" \
            --exclude tests \
            --exclude __pycache__ \
            --exclude .pytest_cache
          mkdir -p "$OUT/dashboard/dist"
          cp "$OUT/dashboard/src/index.js" "$OUT/dashboard/dist/index.js"
          python - <<PY
          import json
          from pathlib import Path
          root = Path('$OUT')
          manifest = json.loads((root/'dashboard/manifest.json').read_text('utf-8'))
          assert manifest['entry'] == 'dist/index.js'
          assert manifest['api'] == 'plugin_api.py'
          assert manifest['icon'] == 'Activity'
          for rel in [
              'plugin.yaml', '__init__.py', 'install.ps1', 'doctor.ps1',
              'compatibility.py', 'dashboard/manifest.json',
              'dashboard/dist/index.js', 'dashboard/dist/style.css',
              'dashboard/plugin_api.py', 'management/overview.py',
              'task_center/service_v3.py', 'wechat/adapter.py',
              'wechat/runtime.py', 'platforms/wechat-desktop/plugin.yaml'
          ]:
              assert (root/rel).is_file(), rel
          assert not (root/'dashboard/compat_api.py').exists()
          ui = (root/'dashboard/dist/index.js').read_text('utf-8')
          api = (root/'dashboard/plugin_api.py').read_text('utf-8')
          for token in ('Overview', 'Agents', 'Projects', 'Tasks', 'WeChat', 'Safe dry-run test'):
              assert token in ui
          for route in ('/wechat/health', '/wechat/status', '/wechat/dry-run'):
              assert route in api
          PY
          (cd release && zip -qr "hermes-extensions-v${VERSION}.zip" "hermes-extensions-v${VERSION}")
          cd release
          sha256sum "hermes-extensions-v${VERSION}.zip" > "hermes-extensions-v${VERSION}.zip.sha256"

      - name: Validate release archive isolation
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          unzip -l "release/hermes-extensions-v${VERSION}.zip" > /tmp/archive.txt
          ! grep -q "src/openakita" /tmp/archive.txt
          ! grep -q "apps/setup-center" /tmp/archive.txt
          ! grep -q "/tests/" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/plugin.yaml" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/dashboard/dist/index.js" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/dashboard/dist/style.css" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/dashboard/plugin_api.py" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/management/overview.py" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/task_center/service_v3.py" /tmp/archive.txt
          grep -q "hermes-extensions-v${VERSION}/wechat/runtime.py" /tmp/archive.txt

      - uses: actions/upload-artifact@v4
        with:
          name: hermes-extensions-v${{ steps.version.outputs.version }}
          path: |
            release/hermes-extensions-v${{ steps.version.outputs.version }}.zip
            release/hermes-extensions-v${{ steps.version.outputs.version }}.zip.sha256

      - name: Publish GitHub Release for tag builds
        if: startsWith(github.ref, 'refs/tags/hermes-extensions-v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          TAG="hermes-extensions-v${VERSION}"
          test "$GITHUB_REF_NAME" = "$TAG"
          gh release create "$TAG" \
            "release/hermes-extensions-v${VERSION}.zip" \
            "release/hermes-extensions-v${VERSION}.zip.sha256" \
            --title "Hermes Extensions v${VERSION}" \
            --notes "Hermes Extensions v${VERSION} adds the complete Management Center UI: Overview, searchable Agent/Project/Task management, capability-aware Project fallback, full lifecycle controls, responsive dialogs, WeChat gateway/desktop health, recent/unread chats, and fail-closed dry-run testing. It retains transactional Windows install/rollback, hardened cross-process WeChat automation, fair Task Center scheduling, and Hermes v0.16 compatibility."

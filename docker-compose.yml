services:
  openakita:
    build: .
    restart: unless-stopped
    expose:
      - "18900"
    volumes:
      - ./data:/app/data
    environment:
      API_HOST: 0.0.0.0
      API_PORT: 18900
    networks:
      - default

networks:
  default:
    driver: bridge
